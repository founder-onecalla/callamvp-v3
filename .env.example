# Supabase (Frontend)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# OpenAI Realtime API Audio Bridge (for low-latency voice AI)
# Deploy audio-bridge/main.ts to Deno Deploy, then set the URL here
# When set, uses OpenAI Realtime API for ~300-500ms voice latency
VITE_AUDIO_BRIDGE_URL=https://your-bridge.deno.dev

# Audio Relay (legacy - for Listen In feature only)
# Only needed if not using Audio Bridge above
VITE_AUDIO_RELAY_URL=wss://your-relay.deno.dev

# These are set in Supabase Dashboard > Edge Functions > Secrets
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# OPENAI_API_KEY=sk-...
# TELNYX_API_KEY=KEY...
# TELNYX_PUBLIC_KEY=...
# TELNYX_CONNECTION_ID=...
# TELNYX_PHONE_NUMBER=+1...
# AUDIO_BRIDGE_URL=https://your-bridge.deno.dev (same as frontend, for backend)
